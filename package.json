{
  "name": "sesa-web-app",
  "private": true,
  "description": "Local MongoDB helpers — backend/frontend stay in their folders",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f mongo",
    "agent:dev": "bash -lc 'cd python-agents && ./run-dev.sh'",
    "dev:all": "concurrently -k -n agent,api,web -c cyan,magenta,green \"npm run agent:dev\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "react-icons": "^5.6.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "ngrok": "^5.0.0-beta.2"
  }
}
